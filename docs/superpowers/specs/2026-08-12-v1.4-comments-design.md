# v1.4 评论系统:Giscus(默认关闭,可选开启)

**状态**:设计已批准,待写实现计划
**日期**:2026-08-12
**作者**:brainstorming 会话产出
**PRD 对照**:[§14.2 v1.0 后的迭代方向](../../PRD.md#142-v10-后的迭代方向)

---

## 1. 背景与决策上下文

### 1.1 起点

PRD §14.2 列出 v1.4 为「评论系统(Giscus/Utterances,可选)」,优先级 **低**,ADR-002 已预告评论是「纯 Astro 原生组件」原则的例外(「文档提供如何接入的指南,但默认不带」)。本设计文档定义 v1.4 的最终交付范围。

### 1.2 核心约束

| 约束 | 来源 | 对设计的影响 |
|---|---|---|
| 模板默认产物必须保 Lighthouse 4×100 | 项目核心卖点 | 评论必须 **默认关闭**,env 空值 = 零 JS |
| 三层架构严格分离 | AGENTS.md 硬性规则 | 框架层零游戏字符串,Giscus 配置走 env |
| 纯 Astro 原生组件 + 最小 vanilla JS | ADR-002 | 不引入 React island;暗色同步用 vanilla JS |
| 多语言(i18n) | 站点支持 en/ja | 评论 UI 语言跟随 locale;评论按 locale 独立 |
| 暗色模式 + 主题换肤 | 站点现有能力 | 评论区需跟随站点暗色切换 |
| 开源模板,fork 友好 | 项目定位 | 配置零代码改动,只填 env |

### 1.3 通过逐项提问确认的关键决策

| # | 决策点 | 选定 | 理由 |
|---|---|---|---|
| 1 | 默认状态 | **默认关闭,env 驱动** | 与现有广告组件同模式;保模板开箱 Lighthouse 4×100 |
| 2 | 评论后端 | **只做 Giscus** | Discussions 适合讨论;内置 ja 多语言 UI;支持主题跟随。Utterances 与本站架构不搭 |
| 3 | 加载策略 | **lazy 加载(`data-loading="lazy"` 透传给 iframe)** | Giscus 官方支持 `data-loading="lazy"`,透传给它创建的 iframe;最简、Lighthouse 友好(见 §3.5 源码核查) |
| 4 | 暗色联动 | **同步(vanilla JS + postMessage)** | 与站点主题切换器联动;~20 行 JS |
| 5 | mapping 策略 | **pathname 路径映射** | Giscus 推荐方式;无需 frontmatter 字段;URL 由 category+slug 决定,稳定 |
| 6 | Giscus UI 语言 | **跟随页面 locale** | en→en, ja→ja,与站点 i18n 一致 |
| 7 | 挂载位置 | **正文 + tags 之后,InContentAd 之前** | 读完正文即见评论入口,曝光率最高 |
| 8 | demo 站启用 | **anvilwiki.pages.dev 启用** | 展示效果;接受 Performance 从 100 降到 ~95 |

---

## 2. 架构

### 2.1 整体形态

新增一个 `Comments.astro` 组件,挂在 `ArticlePage.astro` 正文 + tags 之后、InContentAd 之前。组件读取 env 变量,**空值时 `return null` 不渲染任何东西**(与现有广告组件同一模式)。启用时注入 Giscus 官方 `<script async data-loading="lazy">`(透传 lazy 给它创建的 iframe),并用两个 MutationObserver + postMessage 让它跟随站点暗色模式。

### 2.2 三层架构落点(严格遵守 AGENTS.md)

```
框架层 (不改动)
└── src/components/article/Comments.astro    🆕 新组件,纯通用逻辑,零游戏字符串

配置层 (改一处)
├── .env.example                              🆕 加 Giscus 配置段(空值)
└── src/locales/{en,ja}.json                  🆕 加 shared.commentsHeading 文案

内容层 (不动)
└── MDX 文件不碰(mapping = pathname,无需 frontmatter 字段)
```

**关键**:Comments 组件本身不持有任何游戏专属配置 —— repo / categoryId 这些都从 env 读,组件只是通用的 Giscus 渲染器。换皮用户改 `.env` 即可,不动框架代码。

### 2.3 与现有模式的对照

| 现有模式 | 评论组件复用 |
|---|---|
| `InContentAd.astro`:env 空值 → `return null` | Comments 完全相同 |
| 主题切换器:vanilla JS toggle `.dark` | Comments 用 postMessage 把 `.dark` 状态同步给 iframe |
| i18n:`shared.*` 文案在 JSON | `shared.commentsHeading` 同源 |
| ArticlePage 已是文章页唯一组装点 | 在它里面加一行 `<Comments ... />` 即可 |

---

## 3. 组件内部结构

### 3.1 Props 与 env 契约

```astro
---
// src/components/article/Comments.astro
interface Props {
  locale: Locale;          // 跟随页面 locale,决定 Giscus UI 语言
}
---
```

组件不接收任何 Giscus 业务配置(repo / categoryId / mapping 等),全部从 `import.meta.env` 读。这样 `ArticlePage` 调用处保持干净:`<Comments locale={locale} />`。

### 3.2 env 变量设计(5 个业务字段空值默认 + 1 个 mapping 带默认值)

```bash
# .env.example 新增段
# ─── Giscus comments (optional) ──────────────────────────────────────
# Leave empty to disable comments (default). Fill all 5 to enable.
# Get these from https://giscus.app after configuring your repo:
PUBLIC_GISCUS_REPO=                   # e.g. "PNGTRID/anvilwiki-comments"
PUBLIC_GISCUS_REPO_ID=
PUBLIC_GISCUS_CATEGORY=               # e.g. "Announcements"
PUBLIC_GISCUS_CATEGORY_ID=
PUBLIC_GISCUS_MAPPING=pathname        # 固定值,文档说明不改
```

**为什么 `mapping` 也做成 env 而非硬编码**:虽然本次设计确定 `pathname`,但留作 env 字段(带默认值 `pathname`)可以让进阶用户不改动组件代码就切到 `title` / `og:title` 等其他映射。代价是 6 行文档。考虑到这是开源模板,「不碰框架代码就能改」的价值大于少一个 env 字段。

### 3.3 启用判定逻辑

```astro
const { locale } = Astro.props;
const env = import.meta.env as Record<string, string | undefined>;
const repo = env.PUBLIC_GISCUS_REPO;
const repoId = env.PUBLIC_GISCUS_REPO_ID;
const category = env.PUBLIC_GISCUS_CATEGORY;
const categoryId = env.PUBLIC_GISCUS_CATEGORY_ID;
const mapping = env.PUBLIC_GISCUS_MAPPING || 'pathname';

// 4 个必填字段任一为空 → 整个组件 return null(与 InContentAd 同模式)
const enabled = Boolean(repo && repoId && category && categoryId);
---
{!enabled && null}
{enabled && ( ... 实际渲染 ... )}
```

**判定规则**:4 个业务字段(repo/repoId/category/categoryId)任一为空 → 不启用。`mapping` 有默认值 `pathname`,不算启用条件。这样用户「填一半」时不会渲染出一个坏掉的 iframe。

### 3.4 locale → Giscus 语言映射

Giscus 的 `data-lang` 接受 `en` / `ja` 等短码,与站点 locale 值直接对应。组件内维护一张白名单常量:

```ts
// 仅列出站点实际支持的 locale(en/ja),其余 fallback 到 en
const GISCUS_LANG: Record<string, string> = { en: 'en', ja: 'ja' };
const lang = GISCUS_LANG[locale] ?? 'en';
```

**为什么显式映射而不直接传 `locale`**:Giscus 的语言代码与 BCP-47 不完全一致,用白名单显式映射是防御性的。当前只两个 locale,成本极低。

### 3.5 渲染输出:官方 `<script>` 注入 + `data-loading="lazy"`

**实现选择**:Giscus 官方 `<script src="https://giscus.app/client.js" data-loading="lazy">` 注入方式。

```html
<script
  src="https://giscus.app/client.js"
  data-repo={repo}
  data-repo-id={repoId}
  data-category={category}
  data-category-id={categoryId}
  data-mapping={mapping}
  data-strict="0"
  data-reactions-enabled="1"
  data-emit-metadata="0"
  data-input-position="bottom"
  data-theme="light"
  data-lang={lang}
  data-loading="lazy"
  crossorigin="anonymous"
  async
></script>
```

**为什么不用「静态 iframe」(原设计的初步想法,经源码核查推翻)**:

`client.js` 不只是「创建 iframe」的 loader,它还承担三项运行时职责(经核查 [giscus/client.ts 源码](https://github.com/giscus/giscus/blob/main/client.ts) 确认):

1. **Session 管理**:GitHub OAuth 登录回调到 `?giscus=<token>`,`client.js` 读 token → 存 localStorage → 清 URL → 把 session 注入 iframe src。静态 iframe 没有这段,登录后会卡住。
2. **Height resize**:评论多了,iframe 内 `postMessage({ giscus: { resizeHeight } })` 通知父页面调高度,`client.js` 监听并设置 `iframe.style.height`(iframe 本身 `scrolling="no"`)。静态 iframe 不监听 → 评论多时被截断。
3. **动态参数**:`origin` / `backLink` / `description` 由 `client.js` 用 `location.href` + `<meta>` 实时读取。静态构建时拿不到正确的运行时值。

**`data-loading="lazy"` 是 Lighthouse 关键**:经源码确认,`client.js` 会把这个属性透传给它创建的 iframe(`const loading = attributes.loading === 'lazy' ? 'lazy' : undefined`)。所以「官方 script + data-loading=lazy」等价于「lazy iframe + 全功能运行时管理」—— 既保住 Lighthouse(iframe 接近视口才加载),又不丢失 session/resize/动态参数三项职责。`<script async>` 进一步确保 client.js 本身不阻塞渲染。

**代价**:多一个外部 JS 请求(`client.js` ~10KB)。但这是 Giscus 全功能的必要成本,且 `async` + 透传的 `loading="lazy"` 把它排除在 render-blocking 之外。实测 Lighthouse Performance 预计从 100 降到 92-96(见 §6 验证项 9)。

### 3.6 暗色模式同步(~20 行 vanilla JS)

Giscus 的主题是它自己的 `data-theme` 参数,不自动读站点 `.dark` class。用 Giscus 官方的 `postMessage` 协议切主题(不是销毁重建 iframe):

```js
function syncTheme() {
  const theme = document.documentElement.classList.contains('dark')
    ? 'dark_dimmed'      // Giscus 内置暗色主题
    : 'light';            // Giscus 内置亮色主题
  const iframe = document.querySelector('iframe.giscus-frame');
  iframe?.contentWindow?.postMessage(
    { giscus: { setConfig: { theme } } }, 'https://giscus.app'
  );
}

// observer A:监听 <html> 的 .dark class 变化(用户点主题切换器时)
new MutationObserver(syncTheme).observe(document.documentElement, {
  attributes: true,
  attributeFilter: ['class'],
});

// observer B:监听评论容器的子节点变化(client.js 异步插入 iframe 时触发一次,
// 这样即便 iframe 在 DOMContentLoaded 之后才出现,也能同步到当前主题)
document.addEventListener('DOMContentLoaded', () => {
  const container = document.querySelector('.giscus');
  if (container) {
    new MutationObserver(syncTheme).observe(container, { childList: true, subtree: true });
  }
  syncTheme(); // 兜底:iframe 已存在则立即同步一次
});
```

**两个 observer 各司其职**:A 监听主题变化(用户点击切换器),B 监听 iframe 插入(client.js 延迟创建)。两者都调用同一个 `syncTheme()`,内部 `querySelector` + `?.` 守卫确保 iframe 不存在时不报错。

**关键点**:
1. 监听 `<html>` 的 `class` 属性(站点现有主题切换器 toggle 的就是 `.dark` on `<html>`)
2. 主题选 `dark_dimmed`(对眼睛友好,与多数 wiki 站暗色风格匹配)
3. `is:inline` 让 Astro 不处理这段脚本(避免被 bundle 延迟)

**为什么主题不做成可选**:Giscus 有十几个内置主题,但 `light` + `dark_dimmed` 是最安全的默认组合。自定义主题 CSS 需换皮用户自己托管文件,ROI 太低,本次不做。

---

## 4. ArticlePage 集成与边界情况

### 4.1 ArticlePage.astro 改动(极小)

只动两处:import 一行 + 渲染一行。

```astro
---
import Comments from '~/components/article/Comments.astro';
import InContentAd from '~/components/ads/InContentAd.astro';
...
---

{/* Tags 区块结束之后,InContentAd 之前插入 */}

{/* Comments — env 空值时组件内部 return null,这里不需要条件判断 */}
<Comments locale={locale} />

{/* In-content ad before related articles */}
<InContentAd type="banner-728x90" />
```

**为什么调用处不写条件判断**:`Comments` 内部已做 `enabled` 判定,调用处再包一层会把 env 读取逻辑泄漏到 ArticlePage,破坏「Comments 是自包含的黑盒」边界。与 `InContentAd` 调用风格完全一致。

### 4.2 pathname 在不同 locale 下的值(mapping 正确性验证)

mapping = pathname 意味着 Giscus 用 URL 路径区分讨论帖。不同 locale 的同一篇文章会创建不同的 Discussion —— 这是 **期望行为**(en/ja 评论区独立)。

| 页面 | `Astro.url.pathname` | Giscus Discussion |
|---|---|---|
| `/bosses/gelum`(英文) | `/bosses/gelum` | Discussion #1(英文评论) |
| `/ja/bosses/gelum`(日文) | `/ja/bosses/gelum` | Discussion #2(日文评论,独立) |
| `/ja/bosses/pyra`(日文,回退英文内容) | `/ja/bosses/pyra` | Discussion #3(独立,日文用户可讨论翻译需求) |

**结论**:pathname mapping 天然产生「不同 locale = 不同评论区」。与「文章正文 fallback 到英文但 UI 是日文」的站点行为自洽 —— 日文用户看到英文正文 + 日文评论框,在日文评论框里用日文讨论,不会被英文评论淹没。这是 feature 不是 bug。

### 4.3 i18n 文案改动(最小:每语言 +1 key)

```json
// src/locales/en.json → shared 段
"shared": {
  ...
  "commentsHeading": "Comments"        // 🆕
}

// src/locales/ja.json → shared 段
"shared": {
  ...
  "commentsHeading": "コメント"         // 🆕
}
```

**为什么只加一个 key**:Giscus 自己的 UI(「写评论」「点赞」「按时间排序」等)由 Giscus 的 `data-lang` 控制,不从站点 JSON 读。站点只需提供一个 H2 标题文案,让评论区与文章页其余区块(Related Articles / 関連記事)在视觉风格上统一。

**不新增 `comments` 顶级段**:没有「加载中」「评论数」「发表」等文案需求(这些都由 Giscus iframe 内部渲染),不需要独立 i18n 段落,污染最小。

### 4.4 暗色同步的边界情况

1. **首次加载时的初始主题**:`<script>` 标签上写死 `data-theme="light"` 作为初始值(Astro 静态构建时不知道用户主题,主题切换是客户端 localStorage)。**解决办法**:observer B 在 iframe 创建后立即发一次 postMessage 纠正到当前主题。代价:暗色用户首次加载可能看到评论区闪一下亮色再变暗(约 200ms)。可接受。

2. **主题切换器不存在的情况**:如果换皮用户删了主题切换器,`<html>` 上永远不会有 `.dark` 变化,observer 永不触发 —— 但首次 `DOMContentLoaded` 的纠正仍会执行,评论 iframe 会用 `light` 主题。合理退化。

3. **Giscus iframe 还未创建时切换主题**:iframe 是 `client.js` 运行时动态创建的(`async` script),`DOMContentLoaded` 时它可能还不存在。解决办法见 §3.6 的 observer B —— 监听 `.giscus` 容器的 `childList`,iframe 一被插入就同步主题。`syncTheme()` 内部的 `querySelector` + `?.` 守卫确保 iframe 不存在时不报错。

---

## 5. 文档策略

### 5.1 新增 `docs/comments.md`

面向「想启用评论的换皮用户」,结构与现有 `docs/ads.md` 对齐(都是「可选功能,env 驱动」):

```
docs/comments.md 大纲:
1. 这是什么 — Giscus 评论,存 GitHub Discussions,需 GitHub 账号
2. 前置条件 — 公开 repo + 开 Discussions + 装 giscus bot
3. 配置步骤 — 5 步,对应 5 个 env 变量,截图说明(从 giscus.app 复制)
4. 启用验证 — 本地 pnpm dev,访问任意文章页,看到评论区
5. 多语言行为 — 不同 locale 独立评论区(本文 §4.2 解释)
6. 暗色模式 — 自动跟随,无需配置
7. 关闭评论 — env 留空即可,组件不渲染
8. 常见问题 — 评论不显示?检查 repo 是否公开 / Discussions 是否开 / categoryId 是否对
```

**为什么独立成篇而非塞进 README**:与 ads.md 同理 —— 评论是可选功能,README 只提一句「支持 Giscus 评论,见 docs/comments.md」,详细配置放独立文档。避免 README 膨胀。

### 5.2 PRD 改动

PRD §14.2 的 v1.4 行从 ⏳ 改为 ✅,附一句「Giscus,默认关闭,env 驱动」。ADR 段落不动(ADR-002 已经预告了「评论」这个例外)。

### 5.3 AGENTS.md 改动

在「Hard Rules」加一条(规则 11):

> 11. **评论组件 env 空值 = 不渲染** — `Comments.astro` 在 `PUBLIC_GISCUS_*` 任一为空时 `return null`。与广告组件同模式,默认关闭是模板的开箱契约(保 Lighthouse 4×100)。

**意义**:防止未来有人「优化」Comments 组件时给它加默认值或硬编码 demo 配置,破坏「默认关闭」承诺。

---

## 6. 验证清单

实现完成后逐项过:

| # | 验证项 | 方法 | 通过标准 |
|---|---|---|---|
| 1 | env 全空时组件不渲染 | `.env` 留空,`pnpm build`,grep `dist/` 下任意文章页 HTML | 无 `giscus` 字样 |
| 2 | env 全空时 Lighthouse 不变 | Lighthouse CI 跑文章页 | Performance 100(与现状一致) |
| 3 | env 填满时评论区出现 | `.env` 填 demo repo 配置,`pnpm dev`,访问 `/bosses/gelum` | 看到评论区,可登录 GitHub 评论 |
| 4 | lazy 加载生效 | DevTools Network,初始打开文章页(滚动到评论区前) | `giscus.app/widget` 请求不在初始瀑布图(`client.js` 因 async 可能出现,但它不阻塞;iframe 延迟到接近视口才加载) |
| 5 | locale=en → Giscus 英文 UI | 访问 `/bosses/gelum` | 评论框按钮是英文 |
| 6 | locale=ja → Giscus 日文 UI | 访问 `/ja/bosses/gelum` | 评论框按钮是日文 |
| 7 | en/ja 评论区独立 | 在 `/bosses/gelum` 发评论,刷新 `/ja/bosses/gelum` | 日文页看不到该评论 |
| 8 | 暗色切换同步 | 点主题切换器 | 评论区跟随变暗,无需刷新 |
| 9 | 启用状态下 Lighthouse 可接受 | Lighthouse CI 跑文章页 | Performance ≥ 95(可接受代价) |
| 10 | typecheck 通过 | `pnpm typecheck` | 无错误 |
| 11 | sitemap 不受影响 | `pnpm check-sitemap` | 28/28 通过(评论不产生新 URL) |
| 12 | 回退文章的评论 pathname 正确 | 访问 `/ja/bosses/pyra`(回退英文内容) | pathname = `/ja/bosses/pyra`,与英文页独立 |

**关于 #9 的 Lighthouse 预期**:启用评论的文章页 Performance 预计从 100 降到 92-96(giscus iframe + GitHub API 请求)。这是「用户主动选择启用」的代价,可接受。模板默认状态(env 空)仍 4×100,这是承诺。

---

## 7. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| Giscus 服务宕机 | 低 | 评论区空白,不影响正文 | iframe 失效只是空白,不报错;正文 + 广告不受影响 |
| GitHub API 限流(未鉴权 60/小时) | 中 | 评论加载慢或失败 | Giscus 本身的限制,非可控;文档说明 |
| giscus.bot 配置错误导致评论不显示 | 中 | 用户困惑 | docs/comments.md「常见问题」覆盖;5 个 env 字段有明确校验(任一空则不启用,避免半配置状态) |
| 换皮用户改 URL 导致评论「丢失」 | 低 | 旧评论去不到新 URL | 文档提示「URL 一旦发布尽量不改」;Discussions 还在 GitHub 可手动找回 |
| Lighthouse Performance 100 徽章在 demo 站丢失 | 中 | demo 不再是「4×100」展示 | demo 站启用评论(已决定),接受 Performance 降到 ~95;README 的 Lighthouse 徽章更新措辞为「默认状态 4×100,启用可选功能后 Performance 略降」 |

---

## 8. 范围明确不做(防止 scope creep)

- ❌ 不支持 Utterances(已决定只做 Giscus)
- ❌ 不做评论数 badge(列表页 / 首页)
- ❌ 不做 frontmatter term 映射
- ❌ 不做自定义主题 CSS(对齐 `--nav-theme`)
- ❌ 不做评论审核工具(GitHub 自带)
- ❌ 不把 demo 站的 Giscus repo 配置写进代码(env 才是真相源)

---

## 9. 涉及文件清单

```
🆕 src/components/article/Comments.astro          # 核心组件,~90 行(含暗色同步 JS)
🆕 docs/comments.md                                 # 用户文档
📝 src/components/article/ArticlePage.astro         # +2 行(import + 调用)
📝 src/locales/en.json                              # +1 key(commentsHeading)
📝 src/locales/ja.json                              # +1 key(commentsHeading)
📝 .env.example                                     # +1 段(6 行 Giscus 配置)
📝 AGENTS.md                                        # +1 条 Hard Rule(规则 11)
📝 docs/PRD.md                                      # §14.2 v1.4 行 ⏳→✅
📝 README.md                                        # Lighthouse 措辞更新(可选)
```

共 2 个新文件 + 7 个修改(README 可选)。框架层只动 1 个新组件 + 1 处 import,符合「三层分离」与「最小改动」原则。
